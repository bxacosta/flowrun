{
  "name": "flowrun",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "fix": "ultracite check",
    "check": "ultracite fix",
    "fix:core": "bun run --cwd ./packages/core fix",
    "check:core": "bun run --cwd ./packages/core check",
    "build:core": "bun run --cwd ./packages/core build",
    "test:core": "bun run --cwd ./packages/core test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.5",
    "@types/bun": "latest",
    "typescript": "^5.9.3",
    "ultracite": "7.2.5"
  }
}
